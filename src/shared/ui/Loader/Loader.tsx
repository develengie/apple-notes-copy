import "./Loader.scss";

const Loader = () => {
    return (
        <div className="loader">
            <h1 className="loader__title">Loading...</h1>
        </div>
    );
};

export default Loader;
